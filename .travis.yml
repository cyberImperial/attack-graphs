language:
  cpp
compiler:
  - gcc
python:
  - 3.5
install:
  - sudo apt-get install libboost-all-dev
  - sudo pip install coveralls
script:
  - make test
  - cd database
  - python3 load.py -r
  - cd ..
  - coverage run setup.py test
after_success:
  - coveralls
